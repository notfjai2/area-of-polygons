ASK the user for a shape.

ASK the user for a First measurement.

ASK the user for the second measurement.

If shape is triangle

   return (a × b) ÷ 2
   
Else if shape is circle

   return pi * radius**2
   
Else if shape is square

   return side**2
   
